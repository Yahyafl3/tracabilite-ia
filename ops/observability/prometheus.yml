global:
  scrape_interval: 15s

scrape_configs:
  - job_name: backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["backend:8080"]
    # Activer management.endpoints.web.exposure.include=prometheus en profil prod/local
